numpy==2.2.6
pandas==2.3.0
pydicom==3.0.1
opencv-python-headless==4.10.0.84
scikit-image==0.25.0
# ⚠ Версии ДОЛЖНЫ совпадать с теми, на которых обучены models/*.pkl.
# HistGradientBoosting внутри MeanOfTwo разбирается из pickle другой версией
# sklearn молча, но отдаёт другие числа: на тестовом бедре 0,804 против 0,324,
# и вердикт менялся. Признаки при этом совпадали до последнего знака, так что
# по выходу сервиса поймать это невозможно — только сверкой окружений.
scikit-learn==1.7.2
scipy==1.15.3
openpyxl==3.1.5
# ⚠ torch обязателен: маска бедра (U-Net) и детектор межпозвонковых дисков —
# обученные сети. Без него все кадры бедра уходят в Failure.
# Обычная сборка с PyPI, не CPU-only: по ТЗ финальное тестирование идёт на
# двух H200, и видеокарта ускоряет энкодер эмбеддингов. На машине без
# видеокарты тот же пакет работает на процессоре.
torch==2.5.1
# ⚠ torchvision обязателен, хотя напрямую нигде не импортируется: без него
# Sam2ImageProcessorFast падает на ImportError, энкодер эмбеддингов молча не
# поднимается, и модели недосчитываются восьми признаков. Ловилось только
# внутри образа — локально пакет стоит как зависимость чего-то ещё.
torchvision==0.20.1
# transformers нужен энкодеру SAM для эмбеддингов; без него сервис работает,
# но модели недосчитаются восьми признаков, на которых обучены
# SAM 3 нужен для оси позвоночника; та же версия, что в офлайн-расчёте.
transformers==5.13.1
safetensors==0.8.0
pillow==11.0.0
# ⚠ Разбор решения на странице (ТЗ, п. 1 и 2.6): вклад признаков в голову.
# Объясняется бустинговая половина MeanOfTwo — TreeExplainer работает с ней.
# Без пакета сервис работает: разбор голов просто не показывается, правила и
# вердикт остаются.
shap==0.48.0
# API пакетной обработки — требование ТЗ, пункт 3.2
fastapi==0.115.6
uvicorn==0.34.0
python-multipart==0.0.20
